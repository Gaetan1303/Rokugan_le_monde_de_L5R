import app from './server.js';
const PORT = process.env.PORT || 3000;
app.listen(PORT, () => {
    console.log(` Le Serveur GameMaster L5R démarré sur le port ${PORT}`);
});
//# sourceMappingURL=index.js.map